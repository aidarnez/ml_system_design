repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.4
    hooks:
      # Linter
      - id: ruff-check
        types_or: [python, pyi]
        args: [--fix]
      # Formatter
      - id: ruff-format
        types_or: [python, pyi]
